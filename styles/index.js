import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  schoolsContainer: {
    borderBottomColor: 'lightgrey',
    borderBottomWidth: 0.7,
  },
  schoolsInnerContainer: {
    display: 'flex',
    flexDirection: 'row',
    flexWrap: 'wrap',
    justifyContent: 'center',
    alignItems: 'center',
    textAlign: 'center',
    padding: '2%',
    marginLeft: '3%',
    marginRight: '3%',
  },
});
